import styles from '@/styles/SkillLayout.module.scss';
export default function SkillLayout(props: { setPage: Function }) {
    return(<>
    <div className={styles.wrapper}>
        
    </div>
    </>)
}